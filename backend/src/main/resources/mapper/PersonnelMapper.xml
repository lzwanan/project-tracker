<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.tracker.mapper.PersonnelMapper">
    <resultMap id="roleResultMap" type="com.project.tracker.entity.SysRole">
        <id column="role_id" property="id"/>
        <result column="role_name" property="name"/>
        <result column="role_code" property="code"/>
        <result column="role_description" property="description"/>
    </resultMap>
    <select id="selectRolesByPersonnelIds" resultMap="roleResultMap">
        SELECT DISTINCT pr.personnel_id, r.id AS role_id, r.name AS role_name, r.code AS role_code, r.description AS role_description
        FROM personnel_role pr INNER JOIN sys_role r ON pr.role_id = r.id
        WHERE pr.personnel_id IN <foreach collection="personnelIds" item="pid" open="(" separator="," close=")">#{pid}</foreach>
    </select>
</mapper>
