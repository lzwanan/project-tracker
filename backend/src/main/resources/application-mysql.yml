spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_tracker?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8mb4
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ${MYSQL_PASSWORD:root}
  sql:
    init:
      mode: never
